const Speller = require("../lib/logger");
const {expect} = require("chai");
require("mocha");

describe("Logger should", () => {
	it("fill this test", async () => {
		expect(true).is.equal(true);
	});
});

