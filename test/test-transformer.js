const Transformer = require("../lib/transformer");
const {expect} = require("chai");
require("mocha");

describe("Transformer should", () => {
	it("fill this test", async () => {
		new Transformer();
		expect(true).is.equal(true);
	});
});

